<Window x:Class="UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <ListView x:Name="view" FontSize="16" Background="LightBlue">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header = "ID" DisplayMemberBinding="{Binding Path = Id}"/>
                    <GridViewColumn Header = "Pair" DisplayMemberBinding="{Binding Path = Pair}"/>
                    <GridViewColumn Header = "Min Value" DisplayMemberBinding="{Binding Path = MinVal}"/>
                    <GridViewColumn Header = "Max Value" DisplayMemberBinding="{Binding Path = MaxVal}"/>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
